# whoami

- Execute

    ```bash
    # Displays info about me
    $ npx luisnquin
    ```

- Or just read this:

    ```nix
    {
      me = rec {
        os = "NixOS";
        tps = "🤨";
      };
    }
    ```
